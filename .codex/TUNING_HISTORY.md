# Tuning History

## TUNE-0001 — Domain relation feature challenger v1 audit

Hypothesis:
partner/cặp-50, bộ, bóng, chạm and tổng features may improve cầu-kèo model probability estimates.

Change:
Merged PR #27 added five grouped challengers (`partner_cap50`, `bo`, `bong`, `cham`, `tong`) with four chronological folds and point-estimate Brier/LogLoss gating.

Baseline:
production `cau_keo_ml` champion.

Evaluation period:
history available through 2026-09-01; exact fold boundaries persisted by PR #27 artifacts.

Walk-forward configuration:
4 folds; 30-day calibration and roughly 30-day validation blocks where history allowed.

Metrics before:
PR #27's persisted comparison metrics.

Metrics after:
At the audit point no challenger was production-active. The Đề final point estimate had approximately Brier skill `2.806679e-05` and LogLoss skill `3.542191e-04`, below the prior `1e-4` dual threshold because Brier did not pass. These values are not accepted as clean feature effects because baseline/challenger seeds/samples differed.

Confidence interval:
none in v1 — this absence is a scientific defect, not evidence of significance.

Result:
rejected

Production impact:
none at audit point; `domain_trust=0`.

Reason:
comparison was confounded by different sampling/model seeds, had no clustered bootstrap, and Đề evaluation did not match served normalization.

Last verified: 2026-09-02

## TUNE-0002 — Scientific domain challenger v2

Hypothesis:
a domain feature family should affect production only if its improvement survives controlled same-sample walk-forward evaluation and draw-cluster bootstrap uncertainty.

Change:
- split `partner` and `cap_50`;
- immutable per-fold sampling/model seed;
- mode-correct Đề categorical scoring;
- individual fold-3 confirmation with clustered bootstrap;
- fixed production trust rather than holdout-tuned trust;
- final fold evaluates the exact production blend;
- active gate requires both CI lower bounds > 0.

Baseline:
unchanged production `cau_keo_ml` champion.

Evaluation period:
to be generated by CI/real-data challenger run on this branch.

Walk-forward configuration:
4 chronological folds; folds 1-2 screening, fold 3 confirmation, fold 4 final production-blend test.

Metrics before:
see TUNE-0001.

Metrics after:
pending actual branch experiment; never fabricate.

Confidence interval:
paired date/draw cluster percentile bootstrap, default 1000 replicates, deterministic seed 20260902, 95% CI.

Result:
inconclusive

Production impact:
none until CI generates a gate with `domain_active=true`; fail-closed baseline is the default.

Reason:
implementation requires real-data execution before any gain/rejection claim.

Last verified: 2026-09-02
